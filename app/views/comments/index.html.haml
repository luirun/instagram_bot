%p#notice= notice
%h1
  Listing Comments -  This photo have #{@comments.count} comments!
%table
  %thead
    %tr
      %th User
      %th Photo
      %th Page
      %th{:colspan => "3"}
  %tbody
    - @comments.each do |comment|
      %tr
        %td= comment.user_id
        %td= comment.photo_id
        %td= comment.content
        %td= link_to 'Show', photo_comment_path(params[:photo_id],comment.id)
        %td= link_to 'Edit', edit_photo_comment_path(params[:photo_id], comment)
        %td= link_to 'Destroy', [@photo, comment], method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Comment', new_photo_comment_path
